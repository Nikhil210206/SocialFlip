{
  "name": "social-flip-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install-all": "npm install --prefix frontend/page1-landing && npm install --prefix backend",
    "dev": "concurrently \"npm run dev --prefix frontend/page1-landing\" \"npm start --prefix backend\""
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
